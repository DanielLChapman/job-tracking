mixin forgotForm()

	form.form(action="/account/forgot" method="POST")
		.form-group
			label(for="email") Email
			input(type="email" name="email" class="form-control")
			<br />
			input.button(type="submit" value="Send a Reset" class="btn btn-primary")